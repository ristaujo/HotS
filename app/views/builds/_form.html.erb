<%= form_with(model: build, local: true) do |form| %>
  <% if build.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(build.errors.count, "error") %> prohibited this build from being saved:</h2>

      <ul>
      <% build.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-group">
      <%= form.label :hero_link %>
      <%= form.collection_select(:hero_id, Hero.all, :id, :name ) %>
    </div>

    <div class="form-group">
      <%= form.label :talent_link %>
      <%= form.collection_check_boxes :talent_ids, Talent.all, :id, :name   do |b| %>
        <div class="row" style='margin:0;'>
          <%= b.label(class: "check_box") do %>
            <div style='width:2%;float:left;'>
              <%= b.check_box(class: "check_box") %>
            </div>

            <div class="col-xs-8" style='padding:0;'>
              <%= b.object.name %>
            </div>       
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <%= form.label :map_link %>
      <%= form.collection_check_boxes :map_ids, Map.all, :id, :name   do |b| %>
        <div class="row" style='margin:0;'>
          <%= b.label(class: "check_box") do %>
            <div style='width:2%;float:left;'>
              <%= b.check_box(class: "check_box") %>
            </div>

            <div class="col-xs-8" style='padding:0;'>
              <%= b.object.name %>
            </div>       
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <%= form.label :counter_link %>
      <%= form.collection_check_boxes :counter_hero_ids, Hero.all, :id, :name   do |b| %>
        <div class="row" style='margin:0;'>
          <%= b.label(class: "check_box") do %>
            <div style='width:2%;float:left;'>
              <%= b.check_box(class: "check_box") %>
            </div>

            <div class="col-xs-8" style='padding:0;'>
              <%= b.object.name %>
            </div>       
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <%= form.label :synergy_link %>
      <%= form.collection_check_boxes :synergy_hero_ids, Hero.all, :id, :name   do |b| %>
        <div class="row" style='margin:0;'>
          <%= b.label(class: "check_box") do %>
            <div style='width:2%;float:left;'>
              <%= b.check_box(class: "check_box") %>
            </div>

            <div class="col-xs-8" style='padding:0;'>
              <%= b.object.name %>
            </div>       
          <% end %>
        </div>
      <% end %>
    </div>

    <div class="form-group">
      <%= form.label :objective %>
      <%= form.text_area :objective, class: 'form-control', placeholder: 'Description' %>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
